# Kimi 任务：产业链栏目双轨并行约束（不可绕过）

> 发件人：Claude  
> 日期：2026-06-06  
> 优先级：P0 — 必须先确认并执行，才能接收后续 Phase 1 任务  
> 对应文档：`docs/INDUSTRY_CHAIN_IMPLEMENTATION_PLAYBOOK_20260606.md`

---

## 一、你的身份

你是 Kimi，Hermass 项目的主力工程实现者。你今天的任务**不是写代码**，而是阅读并确认一组不可绕过的约束。确认后回复"已确认"，我才会发给你 Phase 1 的具体实现任务。

---

## 二、核心约束（红线条款）

### 约束 1：旧栏目绝对保留

- `/chain-assistant` 路由、模板、API、数据构建脚本**一丝不动**
- 禁止改名、禁止重定向、禁止删除
- 禁止把旧页面的逻辑"迁移"到新页面后废弃旧入口

### 约束 2：新栏目只能新增

- 只能新增 `/chain-studio` 路由和相关文件
- 新增文件清单必须在 `web/templates/chain-studio.html` 和 `web/main.py` 的新路由中体现
- 禁止修改 `/chain-assistant` 的任何文件（包括 HTML、API、脚本）

### 约束 3：双轨并行至少 2-4 周

- 在收到明确的"单轨切换指令"前，两个入口必须同时可用
- 你需要在代码层面确保两个入口互不干扰（独立路由、独立模板、独立 API namespace）
- 并行期间，旧栏目继续按原有 cron 任务更新数据

### 约束 4：导航命名区分

- 旧入口显示名称：`产业链助手（旧）`
- 新入口显示名称：`产业链工作台（新）`
- 禁止让两个入口显示相同或近似名称导致用户混淆

---

## 三、为什么有这些约束

1. **降低替换风险**：新栏目在 Phase 3 完成前，候选池质量、判断可信度、页面效率均未经过真实对比验证
2. **支持灰度观察**：我们需要对比新旧两栏目的数据覆盖率、候选质量、加载速度
3. **不阻断旧流程**：产业链助手已有 4 条链、500+ 候选、56 条生产观察记录，不可因新开发而中断
4. **符合 AGENTS.md 规约**：项目层明确禁止直接替换已有入口，必须双轨验证后方可切换

---

## 四、你需要确认的事项

请逐条回复：

| # | 确认项 | 你的回复 |
|---|--------|---------|
| 1 | 你明白 `/chain-assistant` 必须100%保留，不做任何修改 | |
| 2 | 你明白只能新增 `/chain-studio` 及相关文件 | |
| 3 | 你明白两个入口需要同时运行至少2-4周 | |
| 4 | 你明白导航需要区分"产业链助手（旧）"和"产业链工作台（新）" | |
| 5 | 你确认在收到本约束后，不会先动手改 `/chain-assistant` 的任何代码 | |

---

## 五、下一步（你确认后我会发出）

确认完成后，我会立即发出：

- `KIMI_TASK_INDUSTRY_CHAIN_PHASE1_FOUNDATION.md`

Phase 1 的内容包括：
- 新建 `/chain-studio` 路由和最小模板骨架
- 新建 `scripts/build_chain_dynamics.py`
- 新建 `scripts/build_industry_position.py`
- 新建 `scripts/build_chain_event_cross.py`
- 在 `industry_chain_evidence.duckdb` 中写入三张新表
- 保持 `/chain-assistant` 完全不动

---

## 六、违反约束的后果

如果你在没有确认的情况下：
- 修改了 `/chain-assistant` 的任何文件 → 需要回滚，重新从双轨约束开始
- 删除了旧入口的导航或路由 → 视为未通过验收
- 把新栏目命名成与旧栏目相同 → 需要重命名并重新部署

---

**请回复"已确认"，并附上上表 5 个确认项的逐条答复。**
