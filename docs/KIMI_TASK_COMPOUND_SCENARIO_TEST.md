# KIMI 任务 — 支线 A 复合场景回归测试

版本：v1.0  
日期：2026-05-31  
执行对象：KIMI  
前置条件：支线 A（watch_command + industry_scan 复合编排）已由 Codex 部署到生产  
目标：验证复合场景功能，确保不影响已有的 9 项测试

---

## 0. 背景

观象 AI 助手新增「复合场景」能力——当用户一句话包含两个意图时，系统同时跑两条场景链并合并结果。

首批支持：**盯盘 + 行业扫描**。例如"帮我盯着 000021，它所在的行业有什么变化"，期望同时返回盯盘任务卡片和行业分析。

---

## 1. 测试环境

| 项 | 值 |
|------|------|
| 网址 | `http://console.supertrader.world` |
| 账号 | `hermass-test` |
| 密码 | `Hermass2026!Lab` |

**测试前确认：支线 A 已部署，服务 running。**

---

## 2. 回归测试 — 已有 9 项

先跑一遍已有 9 项测试，确保复合场景改动没有破坏任何现有功能。用与前两轮相同的测试用例。

验收标准：9/9 通过。

---

## 3. 新增测试 — 复合场景

### Test 10：盯盘 + 行业复合

1. 新会话
2. 确保 LLM 增强开关**打开**
3. 输入：`"帮我盯着 000021，这个行业有什么变化"`

验收标准：

| 字段 | 期望 |
|------|------|
| `task_card` | 存在，非 null |
| `remembered_stock_code` | `000021.SZ` 或 `000021` |
| `answer` | 包含行业分析内容（电子行业） |
| `mode_used` | `chat` |
| `provider` | `agently_deepseek` |
| HTTP 状态 | 200 |

### Test 11：纯盯盘不受影响

1. 输入：`"帮我盯着 000021"`
2. 验收：走纯 `watch_command`，`task_card` 存在，`answer` 是盯盘确认用语

### Test 12：纯行业不受影响

1. 输入：`"电子行业怎么样"`
2. 验收：走纯 `industry_scan`，`answer` 是行业分析，无 `task_card`

---

## 4. 输出格式

最终输出 12 项完整测试表格：

```
┌──────┬───────────────┬─────────────────────────────┬──────────────────────────────┐
│ 结果 │ 测试名        │ 关键字段                    │ 备注                         │
├──────┼───────────────┼─────────────────────────────┼──────────────────────────────┤
│  ✅   │ 对话模式      │ ...                         │ —                            │
│  ...  │ ...           │ ...                         │ ...                          │
│  ?   │ 复合场景      │ task_card= answer=           │                              │
│  ?   │ 纯盯盘        │ task_card=                   │                              │
│  ?   │ 纯行业        │ answer=                      │                              │
└──────┴───────────────┴─────────────────────────────┴──────────────────────────────┘
```

新增 3 项（Test 10/11/12）附完整 JSON 响应。

---

## 5. 如果失败

1. 附 HTTP 请求 JSON 和响应 JSON
2. 标明哪个字段不符合预期
3. 不尝试修改代码
