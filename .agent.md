# Server Ops Prompt — Hermass Observer

## 目标实例
- IP: 8.130.125.201
- 区域: cn-wulanchabu
- 实例 ID: i-0jl8707iukibhcrjphwy
- 项目目录: /opt/hermass
- 服务: hermass-console（端口 8020）

## 标准操作提示词（直接发给服务器 AI）

请直接登录 8.130.125.201 并执行以下操作，不改配置、不改代码、只做同步与发布：
1. cd /opt/hermass
2. git fetch origin
3. git reset --hard <commit>
4. systemctl daemon-reload
5. systemctl restart hermass-console
6. systemctl status hermass-console --no-pager
7. curl -sS http://127.0.0.1:8020/health
8. curl -sS http://127.0.0.1:8020/ | head -n 20
9. curl -sS -o /dev/null -w "%{http_code}" http://127.0.0.1:8020/journal

## 验收与回报要求
请只做只读验收，返回固定格式：
- HEAD: <git rev-parse HEAD>
- status: <systemctl status 核心摘要>
- health: <HTTP 状态码 + 标题>
- journal: <HTTP 状态码 + 标题>
- errors: <报错关键字或无>

## 约束
- 禁止进入项目目录外操作
- 禁止修改 systemd、nginx、环境变量
- 若步骤失败，立即停止并返回错误点
